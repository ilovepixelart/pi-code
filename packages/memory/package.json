{
  "name": "pi-code-memory",
  "version": "0.1.0",
  "description": "Persistent per-project memory: index injected each session, memory tool to save and recall facts",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "type": "module",
  "engines": {
    "node": ">=22.19"
  }
}
